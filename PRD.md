# PRD.md — 평면도 자재(랙) 관리 앱

> MVP / Android 전용 / 출시 안 함 / 로컬 우선(서버 없음)
> 작업은 Phase 순서대로. 한 Phase가 동작·확인된 뒤 다음으로 넘어간다.
> 데이터 모델·아키텍처 규칙은 CLAUDE.md 와 동기화된다. 이 문서는 "무엇을 만드는가"를 정의한다.

---

## 1. 제품 한 줄 정의

평면도 위에 도형(랙·공간)을 배치하고, 자재 도형에는 자재(이름·설명·사진)를 **층(layer)으로 쌓아** 등록해, 어디에 무엇이 있는지 보고·검색·학습·PDF 출력하는 도구형 앱이다.

## 2. 핵심 개념 정의 (오해 방지)

- **Project = 하나의 평면도.** 실제 건물의 한 층/한 구역 단위다. 건물이 여러 층이면 Project를 여러 개 만든다.
- **층(Layer) = 랙 안에서 자재가 쌓이는 단(段).** 건물 층이 아니다. 한 자재 도형(=랙 위치)에 자재가 1개 이상 층층이 쌓일 수 있다.
- **도형(Shape)** 은 두 부류다.
    - **자재 도형**: 자재(층 단위)를 담는다. (직사각형·정사각형·ㄴ자·원)
    - **공간 도형**: 자재를 담지 않고 공간을 표시한다. (문·사무실·기타)

## 3. 데이터 모델 (CLAUDE.md 와 동일하게 유지)

```
Project (= 평면도 1개)
 - id, name(편집 가능), createdAt, updatedAt
 - Shape[]

Shape (도형)
 - id, projectId
 - category : 'material' | 'space'
 - type :
     material 일 때 → 'rect' | 'square' | 'L' | 'circle'
     space    일 때 → 'door' | 'office' | 'etc'
 - x, y, width, height, rotation
 - color
 - alias        // 구역 별칭 "A","B" (옵션, 검색 대상)
 - label        // 공간 도형 표시 텍스트. door="문", office="사무실", etc=기본"기타"(편집 가능)
 - Material[]   // category==='material' 일 때만, 0..n, layerOrder 로 정렬

Material (자재)  // 자재 도형에만 속함
 - id, shapeId
 - layerOrder   // 랙 안에서의 층 순서 (위→아래 또는 아래→위, 구현 시 통일)
 - name         // 필수
 - description  // 옵션
 - imageUri     // 옵션, 사용자가 업로드한 이미지를 앱 로컬로 복사한 경로
```

> 칸(Cell)·건물층(Floor)을 별도 엔티티로 두지 않는다. "한 도형에 여러 자재"는 Material 의 layer(층)로 표현한다.

## 4. 도형 종류 (총 7가지)

**자재 도형 (자재를 담음)**

1. 직사각형(rect) — 자유 비율
2. 정사각형(square) — 1:1 고정
3. ㄴ자(L) — bounding box 리사이즈
4. 원(circle) — 지름 1:1

**공간 도형 (자재 없음, 라벨 표시)** 5. 문(door) — "문"이 써진 직사각형 6. 사무실(office) — "사무실"이 써진 직사각형 7. 기타(etc) — 기타 공간용 직사각형, 라벨 기본 "기타"(사용자가 바꿀 수 있음)

> 모든 도형은 위치/크기/색 지정 가능. 자재 도형만 탭 시 자재 패널이 열린다. 공간 도형은 자재 패널이 열리지 않는다(라벨/색만).

## 5. 모드 설계 — Edit vs Viewer (확정안)

**결론: 단일 화면 + 기본 Viewer 모드 + 우상단 "편집" 버튼으로 Edit 모드 진입.**

판단 근거(사용자 관점):

- 이 앱은 평소엔 "어디에 뭐 있나" 보고/검색하는 참조용이고, 편집은 가끔이다.
- 항상 편집 가능 상태면 그냥 보려다 도형을 드래그해 밀어버리는 오조작이 잦다. → Viewer를 기본값으로 두어 캔버스를 안전하게 만든다.
- 그렇다고 화면을 분리하면 "보다가 고치기"가 번거롭다. → 같은 화면에서 토글 한 번으로 즉시 편집. 화면은 하나다.

**Viewer 모드 (기본)**

- 도형 + 자재 이름 표시, 캔버스 이동/확대.
- 자재 도형 탭 → 바텀시트로 그 랙의 층별 자재 표시(읽기 전용). 자재 탭 → 설명·사진까지 펼침.
- 검색 사용 가능.

**Edit 모드 (편집 버튼으로 진입)**

- 도형 팔레트에서 7종 추가, 이동/리사이즈/회전/색/삭제, 별칭 입력.
- 자재 도형 탭 → 바텀시트에서 자재 추가/수정/삭제, 층(layer) 순서 변경, 이미지 업로드.
- "완료"로 Viewer 복귀.

## 6. 자재 패널 (바텀시트)

- 표시는 **바텀시트**로 한다(확정).
- Viewer: 층 목록(자재 이름) → 자재 선택 시 설명·이미지 펼침. 이미지 없으면 텍스트만.
- Edit: 층 추가/삭제/순서변경, 각 자재의 이름(필수)·설명(옵션)·이미지(옵션) 편집. 이미지는 expo-image-picker 선택 → expo-file-system 으로 앱 로컬 복사 → 경로만 저장(갤러리 원본 의존 금지).

## 7. 화면 & 네비게이션

```
[프로젝트 목록]
  - 프로젝트 카드 목록 (이름·썸네일/요약)
  - 새 평면도 생성, 이름 수정, 삭제
  - 전역 검색창 (모든 프로젝트의 자재 대상)
        │ 프로젝트 열기 / 검색 결과 탭
        ▼
[프로젝트 상세 = 캔버스 1화면]
  - 기본 Viewer, "편집" 토글로 Edit
  - 자재 도형 탭 → 바텀시트
  - 검색창 (현재 프로젝트의 자재 대상)
```

- expo-router 로 구성. 상세는 모드와 무관하게 **하나의 화면**이다.

## 8. 검색 (두 군데)

두 검색 모두 **자재 이름**으로 검색한다(부분 일치). 도형 **별칭(alias)** 도 함께 매칭한다.

**(A) 프로젝트 목록 화면 — 전역 검색**

- 모든 프로젝트의 자재를 대상으로 검색.
- 결과 항목: 자재 이름 + 소속 프로젝트 + 도형 정보.
- 결과 탭 → 해당 프로젝트 상세로 이동(Viewer) → 해당 도형의 바텀시트를 열고 그 자재 정보 표시.

**(B) 프로젝트 상세 화면 — 현재 프로젝트 검색**

- 현재 프로젝트의 자재를 대상으로 검색.
- 결과 탭 → 해당 도형으로 이동/하이라이트 → 바텀시트 열고 그 자재 정보 표시.

- 결과 없을 때 빈 상태 메시지.

## 9. 학습 모드 (둘 다 지원)

기존 데이터/뷰 위에 얹는다. 진입 시 자재 이름 라벨을 가린다.

- **위치 맞히기**: 자재 이름을 제시 → 사용자가 평면도에서 그 자재가 있는 도형을 탭 → 정답/오답 피드백.
- **이름 맞히기**: 특정 도형(위치)을 하이라이트 → 사용자가 그 위치 자재 이름을 맞힘(객관식 또는 입력) → 피드백.
- 라운드/점수 등 세부는 Phase에서 확정.

## 10. 엣지케이스 & 빈 상태

- 자재 없는 자재 도형: 바텀시트에 "자재 없음 / 추가하기".
- **사진 파일 유실**: imageUri 가 가리키는 파일이 없으면 플레이스홀더 표시(그래서 반드시 앱 로컬 복사).
- 빈 프로젝트(도형 0개): "도형을 추가하세요" 안내.
- 빈 프로젝트 목록: "새 평면도를 만드세요" 안내.
- Android 뒤로가기: 바텀시트 열림→닫기, Edit 모드→Viewer 복귀, 상세→목록 순으로 합리적 처리.

## 11. 비기능 요구 (MVP 수준)

- 완전 오프라인. 모든 데이터 SQLite, 사진은 앱 디렉터리.
- 도형 수십 개에서 드래그가 버벅이지 않게 reanimated 로 UI 스레드 처리.

## 12. 범위 밖 (만들지 않는다)

- iOS/웹, 인증/계정, 클라우드 동기화·공유, 실시간 협업
- 출시·스토어 설정, 분석/트래킹, 다국어

## 13. 가정 / 결정 사항 (틀리면 알려줄 것)

- 칸(Cell)·건물층(Floor)을 별도 엔티티로 두지 않고, "한 도형 안 여러 자재"를 Material 의 **층(layer)** 으로 통합했다.
- 검색은 자재 이름 + 도형 별칭(alias) 을 매칭한다(친구 요구 3번 별칭 검색 유지).
- 모드는 단일 화면 Viewer 기본 + 편집 토글로 확정.
- Project는 처음부터 여러 개 지원. 자재 패널은 바텀시트. 학습 모드는 위치·이름 둘 다.

---

## Phase 로드맵

### Phase 0 — 세팅 & 프로젝트 토대

- Expo(TS, New Architecture, dev client, prebuild) + 핵심 패키지 설치
- 폴더 구조 + 도메인 타입(src/types) + SQLite 스키마(Project/Shape/Material) + repository 골격
- **프로젝트 목록 화면**: 생성 / 이름 수정 / 삭제, 빈 상태
- 상세 화면 진입(빈 캔버스) + 네비게이션
- **완료 기준**: 프로젝트를 만들고 이름을 바꾸고 열 수 있고, DB가 유지된다.

### Phase 1 — 도형 편집기 코어 (요구사항 1, 도형 7종)

- Viewer/Edit 토글(기본 Viewer, "편집" 버튼)
- Edit에서 7종 도형 추가(자재 4 + 공간 3), 이동/리사이즈/회전/색/삭제/별칭
- 정사각형·원은 1:1 고정 등 종류별 리사이즈 규칙
- 공간 도형은 라벨 표시(문/사무실/기타, 기타는 라벨 편집)
- repository 경유 저장/복원
- **완료 기준**: 7종 도형을 배치·조작·색/별칭 지정하고 앱 재실행 후 유지된다.

### Phase 2 — 자재 + 층(랙 레이어) (요구사항 2, 1-1/1-2 통합)

- 자재 도형 탭 → 바텀시트
- 자재 추가: 이름(필수)·설명(옵션)·이미지(옵션, 로컬 복사)
- 한 도형에 자재 여러 개 = 층(layer)로 쌓기, 층 순서 변경, 수정·삭제
- **완료 기준**: 한 랙(도형)에 자재를 층층이 등록·정렬하고 사진이 보이며 재실행 후 유지된다.

### Phase 3 — Viewer 모드 완성 (요구사항 4·5)

- Viewer에서 도형 + 자재 이름 표시
- 자재 도형 탭 → 바텀시트(읽기전용) 층 목록 → 자재 탭 시 설명·사진 펼침
- 빈 상태/사진 유실 플레이스홀더 처리
- **완료 기준**: 보기 전용으로 랙의 자재들을 사진까지 확인할 수 있다.

### Phase 4 — 검색 (요구사항 3·6)

- 프로젝트 목록 전역 검색 + 프로젝트 상세 내 검색
- 자재 이름 + 별칭 매칭, 결과 탭 → 해당 도형으로 이동 + 바텀시트로 자재 표시
- **완료 기준**: 두 화면 모두에서 자재 이름으로 찾아 해당 위치/자재로 이동한다.

### Phase 5 — PDF 출력 (요구사항 7)

- view-shot 캡처 + 자재 목록 HTML → expo-print 로 PDF 생성·공유
- **완료 기준**: 현재 평면도가 PDF로 떨어진다.

### Phase 6 — 학습 모드 (위치·이름 맞히기)

- 라벨 가린 상태에서 위치 맞히기 / 이름 맞히기 두 모드, 피드백·점수
- **완료 기준**: 두 방식의 퀴즈가 동작한다.

---

## 진행 규칙

- 각 Phase는 독립 동작·확인 후 커밋.
- "가정/결정"(13장)이 실제와 다르면 작업 전 먼저 보고.
- 데이터 모델 변경이 필요하면 임의로 하지 말고 먼저 보고.
