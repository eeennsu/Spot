// Material 도메인 상수 — 테이블명 등. 하드코딩 금지(여기 참조).

export const MATERIAL_TABLE = 'material' as const;

/** 자재 기본 이름 — 추가 시·이름 비우면 이 값. 파일명 자동 채움 판단 기준. */
export const DEFAULT_MATERIAL_NAME = '새 자재' as const;
